use std::collections::HashMap;

pub fn count_chars(text: &str) -> HashMap<char, usize> {
    // TODO() : Implémenter la fonction count_chars qui compte la fréquence
    // d'apparition de chaque caractère dans le texte.
    // Utiliser la méthode fold pour accumuler les résultats dans une HashMap.
}

fn main() {
    // pour tester vos fonctions
}
